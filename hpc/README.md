# HPC Example Scripts

This folder contains example SLURM scripts illustrating how to run the PSC risk prediction workflow on an HPC cluster.

These scripts are provided as templates only. Users should modify:
- repository paths
- input/output locations
- requested CPU/memory/time
- environment/module loading commands

The original study was executed in a cluster-specific environment, so these examples are intentionally simplified for portability.
